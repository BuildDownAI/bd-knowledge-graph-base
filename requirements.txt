# pyproject.toml is the canonical dependency list (setup.sh installs from it,
# via uv or pip -e). This file is kept for external consumers that pip install
# the deps directly (e.g. the orchestrator image build). Keep the two in sync.
rdflib>=7.0
pyshacl>=0.30
python-frontmatter>=1.1
mcp>=1.8,<1.31  # KGB-28: stateless_http/json_response exist from 1.8; tested on 1.29.1 and 1.30.0 — raise the bound only after tests/test_server_http.py passes on the new version
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
pyyaml>=6.0
fastembed>=0.3
# oxigraph backend: see requirements-oxigraph.txt
